// Widow

version = '0.1'

subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.7
    version = '0.1'

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.11'
    }
}